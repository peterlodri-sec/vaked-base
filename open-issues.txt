#7 checker: undeclared-node refs (engine/stream/output) pass `check` (false green) → `lower` emits invalid Nix  [bug]
#8 language/checker: built-in value-namespace + daemon-channel roster (branch B from #7) + lowering attrpath splice
#9 runtime: nullclaw — OS-log sentinel daemon (eBPF evidence / &#39;testifies&#39; layer)
#10 mesh/protocol: MCPJungle as crabcc⇄crabcc edge MCP gateway (federation = the delta)
#15 epic: port vakedc Python → Zig (byte-parity, bottom-up)
#16 [Design Issue] Reframe: Shared-Graph Grafting via Position-Independent SHM Arena (Fixing Cross-Actor Pointer Leak)  [design]
#17 epic: Vaked 1.0 — three theories (parallel/immutable/control) + controllable release-driver  [language]
#18 Track B: implement eventd — append-only hash-chained event log + arena snapshot API  [design]
#19 Track C: OTP supervision lowering (0012 §7) — parallel/fiber → runnable supervision tree
#20 [Design] Track D: runtime control plane — replay/rewind/jump/step over eventd + arena  [design]
#23 compiler: MLIR multi-agent topology compilation — `vaked` + `hcp` dialects, AOT passes over the agent graph  [design]
#25 vakedc: top-level decls of different kinds with the same name silently collide in the LPG (keep-first)  [bug,agent]
#27 language: define `workflow` — typed agent-step DAG (swe_af pattern): schema, step conformance, DAG verification  [language]
#28 language: schema-less kinds accept anything — `budget`, `runclass`, `input`, `host`, `observability` need catalog schemas (or removal)  [language]
#35 eventd: hardening backlog from independent reviews (rotation/snapshot, group commit, lock portability, diagnostics)  [design]
#45 tooling: in-house adk-rust PR-review agent (replaces Codex + ad-hoc review subagents)
#50 runtime: Wasmtime / Component-Model worker isolation — memory snapshots + instruction-metered budgets  [design]
#51 lowering: colmena deployment target — `host.deploy` → `gen/colmena/hive.nix` + `task deploy`  [design]
#52 protocol: transport identity &amp; distribution — SPIFFE/SPIRE mTLS + NATS fan-out for HCP/eventd  [design]
#54 v1.1-ideas-of-friday-VBcucu — vaked-agents fleet backlog tracking
#58 fix(eventd/ralph): O(n^2) append + no fsync + no boot verify_chain
#59 fix(ralph): step one-shot never clears (fires every tick) + status.json should be an event-log projection
#86 sandboxd isolation backends: canonical wasm/Wasmtime (#50) vs the briefing&#39;s Firecracker + Bubblewrap
#114 language gap: build-time memo/cache primitive (vs runtime `memory` 0014)  [language,design]
#115 language gap: sequential build pipeline modeling (`supervised-dag` is for parallel runtime, not ordered build stages)  [language,design]
#116 Follow-up: implement `ralphloop` primitive (0017) + closed-loop dogfooding eval harness
#117 Follow-up: grow vakedc-zig (v0.x) to parity + wire it as a differential oracle in CI
#141 🚀 Recruitment: WP3 (Rust/Async Wire Protocol) + WP4 (Zig/eBPF Daemon MVP)  [recruitment-start-here,hiring,WP3,WP4]
#153 swe_af GHA runner — realize the lowered workflow (plan→code→review→publish)  [area/agents,area/ci,type/feature]
#165 WP3: HCP wire protocol implementation [Jun 24–Oct 15]  [WP3,wire-protocol]
#166 WP4: Daemon MVP [Jun 24–Sep 15]  [WP4,daemons]
#167 WP3-S1: hcpbin codec (RFC 0002) [Jun 24–Jul 8]  [WP3,wire-protocol]
#168 WP4-S1: sandboxd skeleton + build [Jun 24–Jul 8]  [WP4,daemons]
#192 smoke: swe-af self-test — create docs/SWE_AF_SMOKE.md
#202 ARP (Agent Register Protocol): model-agnostic primitive layer between raw LLM and reliable agent loop  [experiment,cuc,protocol]
#208 [introspect] No actionable improvement — abstaining this window  [agent]
