# Default Node.js runtime flags for @vaked/openrouter-ts
# Source with: export $(cat .node-options | xargs)
# Or use the npm scripts which bake these in.

# Memory: 4GB heap (tune based on model context size)
NODE_OPTIONS=--max-old-space-size=4096 --enable-source-maps --expose-gc
