# Bun runtime config for @vaked/openrouter-ts
# Bun uses mimalloc as its default allocator — no config needed.

[install]
# Use exact versions
exact = true

[run]
# Smol mode: optimize for memory over speed (good for long-running agents)
smol = false
