[package]
name = "vaked-agents-shared"
version = "0.1.0"
edition = "2024"
rust-version = "1.94"
publish = false
description = "Constants shared across all vaked CI agents"

# Standalone crate — no parent workspace.
[workspace]
