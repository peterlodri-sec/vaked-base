# cargo-deny policy for the pr-review agent. Checked only on version bumps
# (see .github/workflows/pr-review-audit.yml).
[advisories]
version = 2
yanked = "deny"

[bans]
multiple-versions = "warn"

[sources]
unknown-registry = "deny"
unknown-git = "deny"
