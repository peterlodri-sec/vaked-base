{
  "name": "vaked-docs",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Self-hosted documentation site for vaked-base, built with Astro Starlight.",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "npm run sync && astro dev",
    "start": "npm run sync && astro dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.36.0",
    "astro": "^5.13.0"
  },
  "devDependencies": {
    "starlight-links-validator": "^0.18.0"
  }
}
