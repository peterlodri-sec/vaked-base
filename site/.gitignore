# build output
dist/
.astro/

# dependencies
node_modules/

# generated content — synced from the repo's docs trees by scripts/sync-docs.mjs
# (source of truth lives in ../docs, ../vaked, ../protocol — never edit these copies)
src/content/docs/

# environment / logs
.env
.env.production
npm-debug.log*
