# Whale project config — vaked-base
# OpenRouter via DEEPSEEK_API_KEY + custom base_url
model = "deepseek/deepseek-chat"

[api]
base_url = "https://openrouter.ai/api/v1"

[permissions]
default = "ask"

[permissions.web_search]
"*" = "allow"

[permissions.web_fetch]
"*" = "allow"

[permissions.mcp]
# MCP servers in .mcp.json at repo root

[shell]
foreground_wait_default_ms = 15000
foreground_wait_max_ms = 120000

[workflows]
enabled = false
keyword_trigger_enabled = false

[logging]
level = "info"
