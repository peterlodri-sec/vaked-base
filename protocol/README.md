# protocol/

Code + spec subtree for **HCP** (Harness Control Protocol) and its **Litany** reference implementation.

- Normative specs: [`rfcs/`](rfcs/) — start at [`0001-hcp.md`](rfcs/0001-hcp.md).
- Overview, vocabulary, daemon + tool roster: [`docs/protocol/README.md`](../docs/protocol/README.md).

**Currently empty of code.** Implementation directories (wire codec, `.hcplang` compiler, `litany*` tools, the `*d` daemons) are created when each piece enters its design → plan → implementation cycle.
