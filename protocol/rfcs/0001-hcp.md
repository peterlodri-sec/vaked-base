# RFC 0001 — HCP (Harness Control Protocol)

- **Status:** Draft / skeleton (stub)
- **Created:** 2026-06-08
- **Track:** Protocol

## Abstract

HCP is the IPC/wire protocol for agent ↔ harness ↔ tool communication in the Vaked
runtime. It carries brokered tool calls, supervision/control messages, and the
event stream that operator surfaces subscribe to. **Litany** is its reference
implementation. This RFC will define the frame model, wire format, encoding, and
schema language. It is currently a skeleton enumerating the sections to be filled.

## Terminology

| Term | Definition |
|------|------------|
| HCP | This protocol. |
| Litany Wire | The on-the-wire byte protocol (framing + transport rules). |
| Votive Frame | A single HCP message in the frame model. |
| `.hcplang` | Schema / IDL describing frame and message types. |
| `hcpbin` | Canonical binary encoding of frames. |

## 1. Frame model (Votive Frames) — TODO

Define frame header, types (request / response / event / control), correlation,
streaming/chunking, and error frames.

## 2. Wire (Litany Wire) — TODO

Transport assumptions (stdio / unix socket / vsock for unikernels), length
framing, version negotiation, flow control, liveness (`candled`).

## 3. Encoding (`hcpbin`) — TODO

Canonical, deterministic binary encoding. Relationship to the Vaked principle of
determinism and source-mapping.

## 4. Schema language (`.hcplang`) — TODO

Grammar, type system, codegen targets (Zig structs for daemons, BEAM terms for the
control plane). `litanyfmt` formats it.

## 5. Roles / daemons — TODO

Bind `chapterd`, `preceptord`, `reliquaryd`, `candled`, `petitiond`, `oraclefd`
to concrete protocol responsibilities (see [overview](../../docs/protocol/README.md)).

## 6. Security considerations — TODO

Authority model (`preceptord`), capability scoping, replay (`litanyreplay`),
tamper-evidence (hash-chained `eventd`).

## Open questions

- stdio vs socket vs vsock as the baseline transport (vsock matters for the
  [MirageOS unikernel surface](../../docs/language/0010-mirageos-unikernel-surface.md)).
- Relationship to MCP: is `mcp-brokerd` an HCP peer that *speaks* MCP outward, or
  is MCP tunnelled inside Votive Frames?
