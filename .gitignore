# Ralph state (per-model decision loop — append-only; reset on major rewrites)
# Intentionally NOT committed (local agent artifacts)
tools/ralph/state/status.json
tools/ralph/state/tracks.json

# Ralph development (venv, lock file)
# Intentionally NOT committed until Phase 4 (when langfuse integration lands)
# Rationale: ralph is stdlib-only; the lock is committed intentionally in Phase 4
# when langfuse (the 'tracing' extra) is wired in.
tools/ralph/uv.lock
tools/ralph/.venv/

# Claude Code agent worktree scratch dirs (ephemeral isolation workspaces)
.claude/worktrees/

# Benchmark + docs validation cache (local agent artifacts)
.benchmark-results/
.docs-cache/

# Vaked compiler artifacts (parse/check/lower cache)
.vaked/

# Python cache (bytecode, optimized)
__pycache__/
*.pyc
*.pyo
*.pyd

# Rust / Cargo build artifacts
target/

# ── Genesis Ceremony — local session artifacts (not deployed) ──
status_check.txt
deploy_tools.txt
dns_verify.txt
deploy_git_state.txt
genesis_hashes.txt
genesis_seal_hash.txt
deploy_hash_check.txt
gh_auth.txt

# ── Deploy output — generated by vakedc lower ──
deploy_tools/vakedc-lower-test/
deploy/vaked.dev/.git/

# ── macOS ──
.DS_Store

# ── Compiled Go binaries (build on target platform, don't commit) ──
tools/vaked-cli/vaked-cli
tools/vaked-cli/vaked-cli-linux-*

# ── IDE / Editor ──
.vscode/
.idea/
*.swp
*.swo
*~

# ── Transient research / agent scratch dirs ──
.agents/
.repowise/
.playwright-mcp/
.crabcc/
.claude/workflows/
.claude/ralph-loop.local.md
CLAUDE.original.md

# pytest cache
.pytest_cache/

# ruff cache
.ruff_cache/
