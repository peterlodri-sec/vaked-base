# --- Nix ---
result
result-*
.direnv/
.pre-commit-config.yaml.bak

# --- vakedc output (regenerable LPG: canonical JSON + SQLite) ---
.vaked/

# --- Python (vakedc prototype + tests/spec) ---
__pycache__/
*.py[cod]

# --- Zig (daemons) ---
zig-cache/
.zig-cache/
zig-out/

# --- BEAM / OTP (agent-supervisord) ---
_build/
deps/
*.beam
erl_crash.dump
.elixir_ls/

# --- Rust (CrabCC and friends, if vendored) ---
target/

# --- CrabCC indexes (regenerable; published as release artifacts) ---
.crabcc/
*.crabcc-index

# --- Editors / OS ---
.DS_Store
*.swp
.idea/
.vscode/

# --- Source bundles (contents are committed in-tree; keep the zip out of git) ---
*.zip

# --- Secrets — NEVER commit (Matrix token, env files) ---
.env
*.env
.matrix.env
secrets/

# --- Generated load-test fixtures (regenerate via examples/evaluation/generate_loadtest.py) ---
# Large, fully deterministic outputs of a ~130-line generator; committing them added
# ~99k lines of review-soup. Regenerate on demand: `make -C examples/evaluation loadtests`.
# The 8-worker swe-swarm-loadtest.vaked is hand-written and intentionally kept in-tree.
vaked/examples/swe-swarm-*-workers.vaked

# --- Generated artifacts (spec dashboard) ---
tools/specdash/index.html

# ralph loop live state (regenerated each run)
tools/ralph/state/status.json

# ralph uv artifacts — the loop is stdlib-only; the lock is committed
# intentionally in Phase 4 when langfuse (the 'tracing' extra) is wired in.
tools/ralph/uv.lock
tools/ralph/.venv/
