System Directive: Atomic State Swap

We are initiating a transition from the existing Python-orchestrated swarm layer to native Zig.

Task:
1. Refactor the dogfeed.py loop into native Zig, utilizing in-memory state backends.
2. Implement a shadow-verify mode: The Zig logic must output the same capability-state hashes as the original Python logic.
3. Verification: Generate a diff between the current Genesis Seal and the post-migration state. The delta must be explicitly logged in MIGRATION_LOG.md.
4. Constraint: If the migration logic requires a change to the grammar, you must pause and request a new evolution_hash before proceeding.
