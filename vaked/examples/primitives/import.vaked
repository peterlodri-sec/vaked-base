# Minimal v0.2 example — import statement
# Demonstrates: use "…" import syntax at file level.

use "vaked/schema/builtins"

host exampleHost {
  hostname = "vaked-host-01"
  system   = "x86_64-linux"
}
