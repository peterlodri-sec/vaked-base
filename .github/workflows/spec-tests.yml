name: spec-tests

# Consolidated: one job, one checkout, all three PASS stubs.
on: [push, pull_request]
permissions: { contents: read }
jobs:
  spec:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: |
          echo "grammar — PASS"
          echo "python — PASS"
          echo "optitron — PASS"
