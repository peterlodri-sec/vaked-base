name: Blogger
on:
  push: { branches: [main], paths: ['blog/**'] }
  workflow_dispatch:
jobs:
  publish:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - run: echo "Blogger — PASS"
