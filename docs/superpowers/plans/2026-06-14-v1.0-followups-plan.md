# Plan - V1.0 publication-pipeline follow-ups

Created: 2026-06-14 | Driver: subagent-driven-development | Branch: feat/swe-af-fanout-batch (no commits this run - CLAUDE.md no-commit-without-instruction overrides the skill's per-task commit)

## Context
The V1.0 publication pipeline produced three audited artifacts (0024 review + two whitepaper sections) and a verified Stage-0 differential corpus. Four follow-ups remain, drawn from those artifacts' follow-up lists. Each is independent and touches a disjoint file, so implementers run in parallel; each output gets a review pass and controller verification. No build (vakedc Stage-0 is pure-python and allowed). No git commit/push.

## Tasks

### T1 - Wire corpus into CI (code)
Add a GitHub Actions job that runs `python3 tests/corpus/0024-differential/run_corpus.py` from repo root as a Stage-0 determinism gate. Pure-python (system Python 3), no Nix/Zig/Cargo build, no dev-cx53. Either a new `.github/workflows/corpus-0024.yml` or an added job in an existing language/test workflow - match existing workflow conventions (check `.github/workflows/` first). Trigger on PRs touching `vakedc/**`, `tests/corpus/**`, or `docs/language/0024*`. Job must fail the build if the harness exits nonzero.
Verify: yaml parses; the run step invokes the harness from repo root; trigger paths correct.

### T2 - 0024 canonicalization appendix (doc)
Add an appendix to `docs/language/0024-mlir-lowering-staged-adoption.md` enumerating the non-deterministic-field set for the artifact comparison. Ground truth: Stage-0 vakedc emitter is fully deterministic (sorted keys, no wall-clock/random), so the Stage-0 canonical set is EMPTY today - state that. The one known cross-stage hazard: `provenance.json` embeds the absolute source path + derived `inputsHash`, so a future Stage-0<->Stage-1 compare must exclude those or compare only semantic artifacts (`gen/workflow/*.json`, `gen/eventd.json`, `flake.nix`, `gen/RUNTIME.md`). Keep the contract general for Stage-1. Cross-link the corpus at `tests/corpus/0024-differential/`. ASCII only, no em-dashes.
Verify: appendix present; provenance path hazard documented; matches the corpus README's finding; no em-dashes.

### T3 - RFC status audit for V1.0 gate (research + doc)
Read the status line of each RFC in `protocol/rfcs/` (0001-0007, and any others present). Build an accurate Accepted/Draft/Review status table. Update `docs/papers/v1.0-release-governance.md` gate condition 2 ("RFCs Accepted") and claim C16/follow-up #1 with the REAL current statuses (do not assert Accepted if they are Draft). This is a current-state fact check - tag honestly.
Verify: every RFC status quoted matches the actual file header; the paper no longer implies RFCs are Accepted if they are not.

### T4 - Prompt integrity-tuple metric spec (design)
In `docs/papers/v1.0-artifact-governance.md` (or a short appendix to it), make the "bounded, measured variance" claim (C11/C14) falsifiable: define the concrete metric (which eval, which percentile e.g. p95, what delta threshold), the seed policy, and the pass/fail rule for the prompt integrity tuple *(prompt version, model digest, decode params + seed, eval baseline)*. Replace any remaining hand-wave with a measurable contract. ASCII only.
Verify: the variance claim is now falsifiable (names a metric + threshold + seed policy); ties back to the integrity tuple; no em-dashes.

## Done criteria
All four verified by the controller personally: T1 harness runs in CI config / yaml valid; T2 appendix matches corpus finding; T3 statuses match files; T4 claim falsifiable. No commits.
