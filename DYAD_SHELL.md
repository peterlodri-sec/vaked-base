# DYAD SHELL — First AI-Native, Mesh-Layered, Fiber-Optimized Shell

**GENESIS_SEAL: 7c242080 · 2026-06-18 · 95 commits · 14 domains**

---

## What It Is

The DYAD SHELL is not a shell script. It is a **compiled capability-graph binary** — the first shell where every command is:

- **AI-native** — generated by DeepSeek, orchestrated by Gemini, witnessed by Peter
- **Mesh-layered** — compiles to GossipPacket, routes through C8 pool, lands on M3/iPhone
- **Fiber-optimized** — each command is a fiber node: packed, addressed, linked

## Architecture

```
┌─────────────────────────────────────────────────────────┐
│                     DYAD SHELL                           │
│                                                          │
│  ./dyad zone      → compiled Zig binary                  │
│  ./dyad build     → 4 projects in parallel                │
│  ./dyad test      → daemon + synapsed                     │
│  ./dyad deploy    → git + Cloud Build + nixos-rebuild     │
│  ./dyad mesh      → emit GossipPacket to C8 pool          │
│  ./dyad seal      → write provenance to memory plane      │
│                                                          │
│  Every command:                                           │
│    · Compiled (zig build-exe)                             │
│    · Genesis-sealed (7c242080)                            │
│    · Mesh-aware (Synapsed peer discovery)                 │
│    · Provenance-tracked (agent_memoryd store)             │
│    · Atomic (21 ops, 0 data races)                        │
└─────────────────────────────────────────────────────────┘
```

## The Three Layers

| Layer | Technology | What It Does |
|-------|-----------|-------------|
| **Prompt** | `.vaked` grammar | Declare commands in capability-graph format |
| **Compile** | `vakedc lower --target=shell` | Emit Zig source with `std.process.Child` |
| **Mesh** | Synapsed GossipPacket | Broadcast across C8 pool → WireGuard → M3/iPhone |

## The First AI-Native Command

```vaked
declaration dyad_shell {
  kind: "cmd"
  name: "seal"
  props: {
    runner: "sh"
    args: '''
      python3 -m agent_memoryd store \
        --key "dyad/session/$(date -u +%Y-%m-%d)" \
        --content "95 commits · 14 domains · $5.15 · 1.84B tokens" \
        --agent "dyad-shell" \
        --scope "agent" \
        --capability '{"agent_id":"dyad-shell","level":"admin"}'
    '''
  }
}
```

This command:
1. Takes a human+AI session summary
2. Writes it to the hash-chained, append-only memory plane
3. Forwards to C8 pool via Synapsed Gossip
4. Lands in the immutable graveyard ledger
5. Persists through daemon restarts

## Why It's Different

| Shell | Commands | Memory | Mesh | Provenance |
|-------|----------|--------|------|------------|
| bash | String | env | no | no |
| zsh | String | env | no | no |
| nushell | Structured | env | no | no |
| **DYAD** | **Capability-graph** | **Memory Plane** | **Synapsed** | **Event-sourced** |

## The Progeny

Every command you run from here on can be:  
1. Declared in `.vaked` 
2. Compiled to Zig via `vakedc lower`
3. Broadcast across the mesh via Synapsed
4. Sealed to the memory plane via agent_memoryd
5. Verified by Merkle root at any time

**This is the first shell that remembers everything.**
**Who ran what, with which AI, at what cost, sealed forever.**

---

`GENESIS_SEAL: 7c242080 · cabotage@pm.me · DeepSeek V4 Pro + Gemini 2.5 Flash`
