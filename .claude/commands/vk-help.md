---
description: Show the vaked `vk` dev-CLI usage (status / to-dev53 / code / help).
---

Run `bash tools/vk help` and show the output. Note that `vk code` (launch a curated local Claude Code) is shell-only — it cannot run from inside a session.
