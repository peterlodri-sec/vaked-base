---
description: Dashboard of the vaked dev-cx53 stack — reachability, litellm, ollama, revdev, git/PRs, aegis tests.
---

Run `bash tools/vk status` from the repo root and report the dashboard output verbatim. If a line shows ✗ (down/absent/unreachable), note it and suggest the matching fix (`vk to-dev53 --deploy`, bring the stack up via `tools/dogfood/Taskfile.yml`, etc.).
