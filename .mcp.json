{
  "mcpServers": {
    "vaked-daemon": {
      "command": "curl",
      "args": ["-sf", "http://localhost:9090/health"],
      "description": "Vaked daemon health, models, and chat completion — real-time status from Atlas."
    },
    "vaked-memory": {
      "command": "curl",
      "args": ["-sf", "http://localhost:8420/health"],
      "description": "Memory plane — store/recall/forget/verify, event-sourced agent memory."
    },
    "vaked-docs": {
      "command": "curl",
      "args": ["-sf", "http://localhost:9845/health"],
      "description": "Vaked Docs — our own documentation index, no rate limits, 224 docs."
    },
    "vaked-rag": {
      "command": "curl",
      "args": ["-sf", "http://localhost:9876/health"],
      "description": "Vaked RAG — semantic search across all repo docs + blog posts."
    },
    "vaked-fleet": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-github"],
      "env": {
        "GITHUB_PERSONAL_ACCESS_TOKEN": "${GH_PERSONAL_TOKEN}"
      },
      "description": "GitHub MCP — issues, PRs, code search, Actions for vaked-base."
    }
  }
}
